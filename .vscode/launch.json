{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (WSL)",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}",
      "debugAdapter": "dlv-dap",
      "buildFlags": ["-gcflags=all=-N -l"],
      "dlvFlags": ["--only-same-user=false"],
      "cwd": "${workspaceFolder}"
    }
  ]
}